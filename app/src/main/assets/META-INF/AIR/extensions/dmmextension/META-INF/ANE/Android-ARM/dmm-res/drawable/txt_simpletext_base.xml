<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- focused -->
    <item android:state_focused="true" android:color="@color/whitetextHover" />
    <!-- clicked -->
    <item android:state_pressed="true" android:color="@color/whitetextHover" />
    <!-- default -->
    <item android:color="@color/normaltext" />
</selector>
